// import { ClipLoader } from "react-spinners";

// const Loader = () => {
//   return <ClipLoader color="blue" size={50} />;
// };

// export default Loader;

import { PulseLoader } from "react-spinners";

const Loader = () => <PulseLoader color="red" size={10} />;

export default Loader;